set nocompatible

" enable filetype detection
if has("filetype")
	filetype indent plugin on
endif

" enable syntax
if has("syntax")
	syntax on
endif

" highlight search
set hlsearch

" case-insensitive search
set ignorecase
set smartcase

